# ---------------------------------------------------------------------------
# Live refresh / Reverb (WebSocket) – add these to your backend/.env
# ---------------------------------------------------------------------------
# Enables real-time notification bell and live dashboard updates (no polling).
# Run Reverb with: php artisan reverb:start (or via Supervisor in production).

BROADCAST_CONNECTION=reverb

# Reverb server (Docker uses local-dev-key / local-dev-secret; or run php artisan reverb:install)
REVERB_APP_ID=1
REVERB_APP_KEY=local-dev-key
REVERB_APP_SECRET=local-dev-secret
REVERB_HOST=0.0.0.0
REVERB_PORT=8080
REVERB_SCHEME=http

# Optional: for production with TLS
# REVERB_SCHEME=https
# REVERB_PORT=443
