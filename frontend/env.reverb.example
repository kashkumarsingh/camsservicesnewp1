# ---------------------------------------------------------------------------
# Live refresh / Reverb (WebSocket) – add these to your frontend/.env.local
# ---------------------------------------------------------------------------
# Enables real-time notification bell and live dashboard updates (WebSocket only).
# Values must match backend Reverb config. For Docker dev, use host that reaches Reverb.
#
# Quick setup: append this file into .env.local, or copy env.template to .env.local
# (env.template now includes these Reverb vars). Restart the dev server after changing.

# Master switch for live-refresh via WebSocket (when true, Echo is used; when false, no live updates)
NEXT_PUBLIC_LIVE_REFRESH_WEBSOCKET_ENABLED=true

# Reverb client config (must match backend; Docker uses local-dev-key)
NEXT_PUBLIC_REVERB_APP_KEY=local-dev-key
NEXT_PUBLIC_REVERB_WS_HOST=localhost
NEXT_PUBLIC_REVERB_WS_PORT=8080
NEXT_PUBLIC_REVERB_SCHEME=http

# Production with TLS example:
# NEXT_PUBLIC_REVERB_WS_HOST=ws.yourdomain.com
# NEXT_PUBLIC_REVERB_WS_PORT=443
# NEXT_PUBLIC_REVERB_SCHEME=https
