/**
 * Shared booking constants used across the app (parent dashboard, validation, etc.).
 * Shared booking constants.
 */

/** Minimum session duration in hours (global business rule) */
export const MIN_DURATION_HOURS = 3;
