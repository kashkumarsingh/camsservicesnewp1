/**
 * JS media-query strings aligned with Tailwind default breakpoints.
 * Tailwind `md:` = min-width 768px, so max-md = 767px.
 * Tailwind `lg:` = min-width 1024px, so max-lg = 1023px.
 */
export const MAX_MD_MEDIA = '(max-width: 767px)';
export const MAX_LG_MEDIA = '(max-width: 1023px)';
export const MIN_MD_PX = 768;
export const MIN_LG_PX = 1024;
export const MIN_XL_PX = 1280;
